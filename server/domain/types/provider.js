/**
 * Defines generic provider metadata to capture HTTP constraints like 429s.
 * @typedef {Object} ProviderMetadata
 * @property {string|null} traceId
 * @property {string|null} timestamp
 * @property {number} httpStatusCode
 */

module.exports = {};
