/**
 * @module utils/geo
 * @responsibility Canonical GPS and geometry types shared across all packages.
 */

/**
 * @typedef {Object} LatLng
 * @property {number} lat
 * @property {number} lng
 */

/**
 * @typedef {Object} BoundingBox
 * @property {number} south
 * @property {number} west
 * @property {number} north
 * @property {number} east
 */

// This file only exports JSDoc types for validation and editor support.
export {};
