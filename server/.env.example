
# Provider Config
PROVIDER_TIMEOUT_MS=3000
PROVIDER_RETRY_COUNT=2

# Overpass API (If the primary instance returns errors, https://lz4.overpass-api.de/api/interpreter is a known-working mirror)
OVERPASS_URL=https://overpass-api.de/api/interpreter
